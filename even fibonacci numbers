first_term = 1
second_term = 1
temp = first_term + second_term
sum = temp
print(first_term,second_term, sep = "\n")

while temp < 10:
    first_term = second_term
    second_term = temp
    print(temp)
    sum += temp
    temp = first_term + second_term


print("Sum is :",sum)
